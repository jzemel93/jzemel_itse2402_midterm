/*
function validateForm() {
    //browser already does some validation
    //validate email
    let fEmail = document.forms(["contact"]["fEmail"]);
    //validate telephone #
    let fTel = document.forms(["contact"]["fTel"]);

    //if email & telephone input match patterns
    success();
    //else those input borders change to red if validation fails
    invalidInput();
}
*/
function success() {
    //after form is successfully submitted, insert message as <br>(message) after submit button
    
}
function invalidInput() {

}